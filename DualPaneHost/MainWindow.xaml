<Window x:Class="DualPaneHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="DSH Dual Pane"
        Width="1400" Height="900"
        Icon="favicon.ico"
        WindowStartupLocation="Manual"
        Background="#f3f4f6">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="300" x:Name="colLeft"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="250" x:Name="colRight"/>
        </Grid.ColumnDefinitions>

        <!-- 左：DSH 对话 -->
        <wv2:WebView2 x:Name="wvLeft" Grid.Column="0"/>

        <!-- 可拖分隔条（浅色） -->
        <GridSplitter Grid.Column="1" Width="6"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Background="#d1d5db" ShowsPreview="False"/>

        <!-- 右：历史注入搜索页 -->
        <wv2:WebView2 x:Name="wvRight" Grid.Column="2"/>
    </Grid>
</Window>
